import DashboardPage from "./admin/dashboard/DashboardPage";
import HomePage from "./client/home/HomePage";

const admin = {
  DashboardPage,
};

const client = {
  HomePage,
};

export { admin, client };
